name: secure_qr_verifier_windows
description: Flutter SecureQR verifier for Windows + Web.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.10.5+2
  image: ^4.2.0
  zxing2: ^0.2.1
  qr_code_dart_scan: ^0.8.3
  http: ^1.2.0




dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
